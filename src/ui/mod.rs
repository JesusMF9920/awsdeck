//! Componentes de UI transversales y consistentes en todas las vistas: header
//! (ambiente + breadcrumbs), command bar (`:` y `/`) y overlay de ayuda.

pub mod command_bar;
pub mod header;
pub mod help;
