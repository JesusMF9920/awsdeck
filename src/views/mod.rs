//! El contrato de plugin `View` (síncrono y object-safe) + el `Registry` genérico
//! que conecta vistas por su `id()`. No nombra ningún servicio concreto: las vistas
//! se registran desde `main.rs`.
//!
//! Se llena en el commit "View trait + registry".

pub mod logs;
